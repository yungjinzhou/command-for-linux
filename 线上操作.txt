提交代码，salt用法，
进程大部分关闭
更新代码，/home/work/apmv2/script/
用脚本更新为线上配置,执行脚本后去确认
用脚本批量启动进程或者（/etc/init.d/apm-modelname   start）
启动进程同时观察日志情况 tail -f /home/work/log/apm/apm.log | grep jh01-db5
多启动refreshfile和nonblockquery进程
关闭没有pid文件的进程（查看日期，关闭旧的进程）  kill -s 15 pid_number(安全关闭，防止进程还有任务)

可以用lsof -p pid_number查看进程连接情况(连接到redis等情况)
lsof -i:port